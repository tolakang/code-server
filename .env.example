# Mobile Code Server Environment Variables
# Copy this file to .env and customize as needed.
#
# When deploying on Dokploy with a managed database, set these
# in the Dokploy UI (Settings → Environment Variables). Dokploy
# writes them to .env and Docker Compose injects them via env_file.

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================
# Option 1: Use DATABASE_URL (recommended for Dokploy managed DB)
# DATABASE_URL=postgresql://user:password@host:port/database_name
#
# Option 2: Use individual vars
DB_HOST=your-dokploy-db-service-name
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=change_me_in_production
DB_NAME=mobile_code_server

# =============================================================================
# Server / Runtime
# =============================================================================
PORT=8080
CODE_SERVER_INTERNAL_PORT=8081
NODE_ENV=production

# =============================================================================
# Authentication
# =============================================================================
AUTH_TYPE=none

# =============================================================================
# Security
# =============================================================================
JWT_SECRET=change_me_to_a_random_string
SESSION_SECRET=change_me_to_a_random_string

# =============================================================================
# AI Features
# =============================================================================
AI_PROVIDER=opencode

# OpenCode (local AI proxy)
# AI_OPENCODE_URL=http://localhost:8082

# Anthropic Claude
# AI_CLAUDE_URL=https://api.anthropic.com
# AI_CLAUDE_API_KEY=your_claude_api_key

# OpenAI Codex
# AI_CODEX_URL=https://api.openai.com
# AI_CODEX_API_KEY=your_codex_api_key

# Google Gemini
# AI_GEMINI_URL=https://generativelanguage.googleapis.com
# AI_GEMINI_API_KEY=your_gemini_api_key

# OpenRouter
# AI_OPENROUTER_URL=https://openrouter.ai/api
# AI_OPENROUTER_API_KEY=your_openrouter_api_key

# =============================================================================
# MCP Server
# =============================================================================
MCP_SERVER_ENABLED=true
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=8083

# =============================================================================
# Logging
# =============================================================================
LOG_LEVEL=info
